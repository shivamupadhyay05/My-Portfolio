# Backend

This folder is reserved for the backend API, server logic, and environment configuration.

## Suggested structure

- `server.js` or `app.js`
- `routes/`
- `controllers/`
- `models/`
- `.env` for local secrets only

## Security

Keep all sensitive values in a local `.env` file and never commit them to Git.
